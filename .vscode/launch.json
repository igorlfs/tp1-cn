{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Module",
            "type": "debugpy",
            "request": "launch",
            "console": "integratedTerminal",
            "python": "${workspaceFolder}/.venv/bin/python",
            "module": "src",
            "cwd": "${workspaceFolder}"
        }
    ]
}
